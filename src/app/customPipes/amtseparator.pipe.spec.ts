import { AmtseparatorPipe } from './amtseparator.pipe';

describe('AmtseparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new AmtseparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
